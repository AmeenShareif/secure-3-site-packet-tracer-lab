# Project Status

This page shows what is done, what is in progress, and what is still left for the Packet Tracer project.

## Current status

| Area | Status | Notes |
| --- | --- | --- |
| Concept and scope | Done | The project idea and company story are set. |
| IP plan | Done | VLANs, subnets, and gateways are documented. |
| Device configs | In progress | The HQ router is verified, the HQ switches are configured and linked, the HQ DNS and web servers are built, and the DR router and switch are now configured too. |
| Visuals | In progress | I added diagrams and charts for the portfolio. |
| Packet Tracer build | In progress | The HQ router is up, the HQ access/core trunk is live, the serial module is installed, the HQ DNS/web servers are configured, and the DR router and switch are now live. |
| Testing | In progress | I verified the HQ router boot, interface status, config application, the HQ DNS/web server setup, the DR router and switch configs, and I ran live ping/SSH checks that currently show DHCP and reachability issues. |
| Screenshots | In progress | I captured topology, config, interface status, physical-module evidence, server-service screenshots, DR router/switch CLI screenshots, and new live command-prompt test shots. |
| Final polish | Next | I will tighten the write-up and portfolio presentation. |

## What I am working on now

- Continuing the Packet Tracer build for the remaining devices and WAN links
- Capturing evidence screenshots as each major step works
- Adding the verified results into the repo so the project reads like real student work
- Working through the remaining branch site and WAN verification
- Fixing host addressing and connectivity so the live ping and SSH checks can pass instead of timing out



